// Client-side hooks only
export { useMinimumLoadingTime } from './useMinimumLoadingTime';

// Note: For server-side minimum loading time, use:
// import { withMinimumLoadingTime } from '@/lib/withMinimumLoadingTime';
