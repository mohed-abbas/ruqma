import { ProductPageSkeleton } from '@/components/ui/loading';

/**
 * Loading UI for product page route transitions
 * Shows skeleton layout matching the product page structure
 */
export default function Loading() {
  return <ProductPageSkeleton />;
}
