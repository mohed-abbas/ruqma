/**
 * Loading UI for Our Story page
 * Returns null since page uses static JSON data (no loading state needed)
 * This overrides the parent (root)/loading.tsx HomePageSkeleton
 */
export default function Loading() {
  return null;
}
