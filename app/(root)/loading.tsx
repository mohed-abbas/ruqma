import { HomePageSkeleton } from '@/components/ui/loading';

/**
 * Loading UI for route transitions
 * Shows skeleton layout matching the home page structure
 */
export default function Loading() {
  return <HomePageSkeleton />;
}
